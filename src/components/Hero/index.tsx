import { FormHero, FormContainer, HeroTitle } from './styles'

const Hero = () => (
  <FormHero>
    <FormContainer className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormContainer>
  </FormHero>
)

export default Hero
